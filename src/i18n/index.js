import en from "../data/profile.en";
import es from "../data/profile.es";

export const languages = {
  en, // arajo de sintaxis. En realodad es en(línea 1):en (línea 5)
  es,
};

// en, es de 5 y 6 -> es un objeto contenedor del enlaze importa. Sirve para que sea más facil de invocar.
